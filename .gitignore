.env
node_modules/
.svelte-kit/
build/
.wrangler/
.DS_Store
*.local

# Your working eval labels (the committed example is the template).
# Remove this line if you'd rather version-control your labels.
pipeline/eval/labels.json

# Database backups (data, potentially large — keep out of git; copy elsewhere)
backups/

# Working file for the FollowTheMoney eid-matching workflow (finance-eid-export.js)
finance-eids.csv
